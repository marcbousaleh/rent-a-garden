<h1>Explore the gardens: </h1>

<div class="cards">
  <% @gardens.each do |garden| %>
    <div class="card-garden">
      <%= cl_image_tag garden.photo, height: 200, width: 300, crop: :mpad %>
      <div class="card-garden-infos">
        <div>
          <h2><%= garden.title %></h2>
          <p><%= garden.description %></p>
        </div>
        <h2 class="card-garden-pricing">£<%= garden.price %>/hr</h2>
      </div>
    </div>

    <% if policy(garden).show? %>
      <%= link_to 'Show garden', garden_path(garden) %>
    <% end %>

    <% if policy(garden).edit? %>
      <%= link_to 'Edit', garden_path(garden) %>
    <% end %>

    <% if policy(garden).destroy? %>
      <%= link_to 'Destroy', garden, method: :delete, data: { confirm: 'Are you sure?' } %>
    <% end %>

  <% end %>

</div>

<% if policy(Garden).new? %>
  <%= link_to 'Create a garden', new_garden_path %>
<% end %>

<%= link_to 'Back',gardens_path %>
