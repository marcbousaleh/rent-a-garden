<div class="cards">
    <div class="card-garden">
    <%= cl_image_tag @garden.photo, height: 300, width: 400, crop: :fill %>
    <div class="card-garden-infos">
      <div>
        <h2>Basic Garden</h2>
        <p><%= @garden.description %></p>
      </div>
      <h2 class="card-garden-pricing"><%= @garden.price %></h2>
    </div>
  </div>
</div>

<div class="login-link">
  <%= link_to 'Book this garden', new_garden_booking_path(@garden) %>
</div>

<div class="login-link">
  <%= link_to 'Back', gardens_path %>
</div>
